/docs/
/lib/
/bin/
/.shards/
*.dwarf

# Native package build artifacts (nfpm).
/dist/
/*.deb
/*.rpm

# This is an application: shard.lock IS committed (reproducible builds).

# Claude Code agent worktrees
/.claude/worktrees/

# Local operator configs (the committed examples are the exception).
/config.yaml
/config.yml
/config.json
!/config.example.yaml
!/config.example.json
